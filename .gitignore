# секреты — никогда в репозиторий
.env
*.env
!.env.example

# сборка
/bin/
/dist/
# собранные пакеты Synology
*.spk
# Только собранный бинарник в корне. Без ведущего слеша этот шаблон
# совпадал ещё и с каталогом cmd/dsm-mini — точка входа не попадала в
# репозиторий, и склонированный проект не собирался.
/dsm-mini
web/dist/
web/node_modules/

# редакторы и ОС
.DS_Store
.idea/
.vscode/
