# secrets — never in the repository
.env
*.env
!.env.example

# build output
/bin/
/dist/
# built Synology packages
*.spk
# Only the built binary in the root. Without the leading slash this pattern
# also matched the cmd/dsm-mini directory — the entry point never reached the
# repository and a cloned project did not build.
/dsm-mini
web/dist/
web/node_modules/

# editors and OS
.DS_Store
.idea/
.vscode/
